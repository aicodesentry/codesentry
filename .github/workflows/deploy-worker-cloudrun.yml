name: Deploy Worker (Cloud Run)

on:
  push:
    branches: [main]
    paths:
      - 'services/worker-service/**'
      - '.github/workflows/deploy-worker-cloudrun.yml'
  workflow_dispatch:

concurrency:
  group: cloudrun-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: '${{ vars.GCP_PROJECT_ID }}'

      - name: Validate required config
        run: |
          test -n "${{ vars.GITHUB_SERVICE_URL }}" || (echo "Missing repo variable: GITHUB_SERVICE_URL" && exit 1)
          test -n "${{ secrets.GITHUB_SERVICE_INTERNAL_SECRET }}" || (echo "Missing repo secret: GITHUB_SERVICE_INTERNAL_SECRET" && exit 1)

      - name: Configure Docker auth
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build Worker image
        run: |
          TAG="worker-${GITHUB_SHA::12}"
          IMAGE="us-central1-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ vars.GAR_REPOSITORY }}/worker:${TAG}"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"
          docker build -t "$IMAGE" ./services/worker-service
          docker push "$IMAGE"

      - name: Deploy Worker
        run: |
          gcloud run deploy codesentry-worker \
            --image "$IMAGE" \
            --region '${{ vars.GCP_REGION }}' \
            --project '${{ vars.GCP_PROJECT_ID }}' \
            --platform managed \
            --update-env-vars "PORT=8080" \
            --update-env-vars "GITHUB_SERVICE_URL=${{ vars.GITHUB_SERVICE_URL }}" \
            --update-env-vars "GITHUB_SERVICE_INTERNAL_SECRET=${{ secrets.GITHUB_SERVICE_INTERNAL_SECRET }}" \
            --allow-unauthenticated
